/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getAddressEncoder,
  getBytesDecoder,
  getBytesEncoder,
  getProgramDerivedAddress,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { DICE_DUEL_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLEANUP_STALE_WAGER_DISCRIMINATOR = new Uint8Array([
  91, 79, 225, 111, 98, 183, 190, 208,
]);

export function getCleanupStaleWagerDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    CLEANUP_STALE_WAGER_DISCRIMINATOR,
  );
}

export type CleanupStaleWagerInstruction<
  TProgram extends string = typeof DICE_DUEL_PROGRAM_ADDRESS,
  TAccountPayer extends string | AccountMeta<string> = string,
  TAccountWager extends string | AccountMeta<string> = string,
  TAccountChallengerStats extends string | AccountMeta<string> = string,
  TAccountEscrow extends string | AccountMeta<string> = string,
  TAccountChallenger extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> =
    "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountPayer extends string
        ? WritableSignerAccount<TAccountPayer> &
            AccountSignerMeta<TAccountPayer>
        : TAccountPayer,
      TAccountWager extends string
        ? WritableAccount<TAccountWager>
        : TAccountWager,
      TAccountChallengerStats extends string
        ? WritableAccount<TAccountChallengerStats>
        : TAccountChallengerStats,
      TAccountEscrow extends string
        ? WritableAccount<TAccountEscrow>
        : TAccountEscrow,
      TAccountChallenger extends string
        ? WritableAccount<TAccountChallenger>
        : TAccountChallenger,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CleanupStaleWagerInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type CleanupStaleWagerInstructionDataArgs = {};

export function getCleanupStaleWagerInstructionDataEncoder(): FixedSizeEncoder<CleanupStaleWagerInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLEANUP_STALE_WAGER_DISCRIMINATOR }),
  );
}

export function getCleanupStaleWagerInstructionDataDecoder(): FixedSizeDecoder<CleanupStaleWagerInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCleanupStaleWagerInstructionDataCodec(): FixedSizeCodec<
  CleanupStaleWagerInstructionDataArgs,
  CleanupStaleWagerInstructionData
> {
  return combineCodec(
    getCleanupStaleWagerInstructionDataEncoder(),
    getCleanupStaleWagerInstructionDataDecoder(),
  );
}

export type CleanupStaleWagerAsyncInput<
  TAccountPayer extends string = string,
  TAccountWager extends string = string,
  TAccountChallengerStats extends string = string,
  TAccountEscrow extends string = string,
  TAccountChallenger extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Anyone can call this — permissionless */
  payer: TransactionSigner<TAccountPayer>;
  /**
   * The stale wager to close. Must be Pending + nonce stale.
   * `close = challenger` zeroes the discriminator (prevents resurrection) and sends rent to challenger.
   */
  wager: Address<TAccountWager>;
  /** Challenger's PlayerStats — used to verify staleness */
  challengerStats: Address<TAccountChallengerStats>;
  escrow?: Address<TAccountEscrow>;
  challenger: Address<TAccountChallenger>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export async function getCleanupStaleWagerInstructionAsync<
  TAccountPayer extends string,
  TAccountWager extends string,
  TAccountChallengerStats extends string,
  TAccountEscrow extends string,
  TAccountChallenger extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DICE_DUEL_PROGRAM_ADDRESS,
>(
  input: CleanupStaleWagerAsyncInput<
    TAccountPayer,
    TAccountWager,
    TAccountChallengerStats,
    TAccountEscrow,
    TAccountChallenger,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CleanupStaleWagerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountWager,
    TAccountChallengerStats,
    TAccountEscrow,
    TAccountChallenger,
    TAccountSystemProgram
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? DICE_DUEL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    wager: { value: input.wager ?? null, isWritable: true },
    challengerStats: { value: input.challengerStats ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    challenger: { value: input.challenger ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.escrow.value) {
    accounts.escrow.value = await getProgramDerivedAddress({
      programAddress,
      seeds: [
        getBytesEncoder().encode(new Uint8Array([101, 115, 99, 114, 111, 119])),
        getAddressEncoder().encode(expectAddress(accounts.wager.value)),
      ],
    });
  }
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.wager),
      getAccountMeta(accounts.challengerStats),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.challenger),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCleanupStaleWagerInstructionDataEncoder().encode({}),
    programAddress,
  } as CleanupStaleWagerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountWager,
    TAccountChallengerStats,
    TAccountEscrow,
    TAccountChallenger,
    TAccountSystemProgram
  >);
}

export type CleanupStaleWagerInput<
  TAccountPayer extends string = string,
  TAccountWager extends string = string,
  TAccountChallengerStats extends string = string,
  TAccountEscrow extends string = string,
  TAccountChallenger extends string = string,
  TAccountSystemProgram extends string = string,
> = {
  /** Anyone can call this — permissionless */
  payer: TransactionSigner<TAccountPayer>;
  /**
   * The stale wager to close. Must be Pending + nonce stale.
   * `close = challenger` zeroes the discriminator (prevents resurrection) and sends rent to challenger.
   */
  wager: Address<TAccountWager>;
  /** Challenger's PlayerStats — used to verify staleness */
  challengerStats: Address<TAccountChallengerStats>;
  escrow: Address<TAccountEscrow>;
  challenger: Address<TAccountChallenger>;
  systemProgram?: Address<TAccountSystemProgram>;
};

export function getCleanupStaleWagerInstruction<
  TAccountPayer extends string,
  TAccountWager extends string,
  TAccountChallengerStats extends string,
  TAccountEscrow extends string,
  TAccountChallenger extends string,
  TAccountSystemProgram extends string,
  TProgramAddress extends Address = typeof DICE_DUEL_PROGRAM_ADDRESS,
>(
  input: CleanupStaleWagerInput<
    TAccountPayer,
    TAccountWager,
    TAccountChallengerStats,
    TAccountEscrow,
    TAccountChallenger,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CleanupStaleWagerInstruction<
  TProgramAddress,
  TAccountPayer,
  TAccountWager,
  TAccountChallengerStats,
  TAccountEscrow,
  TAccountChallenger,
  TAccountSystemProgram
> {
  // Program address.
  const programAddress = config?.programAddress ?? DICE_DUEL_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    payer: { value: input.payer ?? null, isWritable: true },
    wager: { value: input.wager ?? null, isWritable: true },
    challengerStats: { value: input.challengerStats ?? null, isWritable: true },
    escrow: { value: input.escrow ?? null, isWritable: true },
    challenger: { value: input.challenger ?? null, isWritable: true },
    systemProgram: { value: input.systemProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.payer),
      getAccountMeta(accounts.wager),
      getAccountMeta(accounts.challengerStats),
      getAccountMeta(accounts.escrow),
      getAccountMeta(accounts.challenger),
      getAccountMeta(accounts.systemProgram),
    ],
    data: getCleanupStaleWagerInstructionDataEncoder().encode({}),
    programAddress,
  } as CleanupStaleWagerInstruction<
    TProgramAddress,
    TAccountPayer,
    TAccountWager,
    TAccountChallengerStats,
    TAccountEscrow,
    TAccountChallenger,
    TAccountSystemProgram
  >);
}

export type ParsedCleanupStaleWagerInstruction<
  TProgram extends string = typeof DICE_DUEL_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Anyone can call this — permissionless */
    payer: TAccountMetas[0];
    /**
     * The stale wager to close. Must be Pending + nonce stale.
     * `close = challenger` zeroes the discriminator (prevents resurrection) and sends rent to challenger.
     */
    wager: TAccountMetas[1];
    /** Challenger's PlayerStats — used to verify staleness */
    challengerStats: TAccountMetas[2];
    escrow: TAccountMetas[3];
    challenger: TAccountMetas[4];
    systemProgram: TAccountMetas[5];
  };
  data: CleanupStaleWagerInstructionData;
};

export function parseCleanupStaleWagerInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCleanupStaleWagerInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 6) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      payer: getNextAccount(),
      wager: getNextAccount(),
      challengerStats: getNextAccount(),
      escrow: getNextAccount(),
      challenger: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getCleanupStaleWagerInstructionDataDecoder().decode(instruction.data),
  };
}
